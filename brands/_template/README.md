# _template

Standard skeleton for one brand. Copy to `brands/<slug>/` (or just let the
pipeline create `<slug>/` directly — it follows this shape). Stubs below show
the expected files; the pipeline overwrites them with real extracted data.

Do not put real brand data in `_template/`. It documents structure only.

See `brands/README.md` for the full layout and retrieval commands.
